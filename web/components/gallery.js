const Gallery = (data) => {
    return (
        <div>CTAOOO</div>
        // <div style={{backgroundImage: `linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5)), url(${});`}}>

        // </div>
    )
}
export default Gallery